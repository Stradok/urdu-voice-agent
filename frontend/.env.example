# Copy to .env and fill in - these are the same SUPABASE_URL / SUPABASE_PUBLISHABLE_KEY
# values from the backend's .env (the publishable key is safe for client-side use by design).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
